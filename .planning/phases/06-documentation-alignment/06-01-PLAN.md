---
phase: 06-documentation-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflow.txt
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "workflow.txt describes exactly 6 phases matching the CLAUDE.md structure"
    - "All commands use 'python src/' format — no conda prefix anywhere"
    - "Non-existent scripts (evaluation_pipeline.py, evaluation_report_generator.py, evaluation_dashboard.ipynb) are absent"
    - "Phase 7 Responsible AI Layer is not listed as a separate phase — it is noted as integrated into Phase 4"
    - "Output artifact names match actual codebase files"
  artifacts:
    - path: "workflow.txt"
      provides: "6-phase execution guide with accurate commands and output files"
      contains: "python src/pubmed_fetcher.py"
  key_links:
    - from: "workflow.txt Phase 4"
      to: "src/medical_verifier.py"
      via: "python src/medical_verifier.py"
      pattern: "python src/medical_verifier\\.py"
    - from: "workflow.txt Phase 6"
      to: "src/report_generator.py"
      via: "python src/report_generator.py"
      pattern: "python src/report_generator\\.py"
---

<objective>
Rewrite workflow.txt to accurately reflect the 6-phase execution pipeline.

Purpose: The current workflow.txt is a corrupted, garbled document with a broken 7-phase structure, non-existent script references, and every command prefixed with a non-existent conda environment path. A contributor following this file would fail at every step.

Output: A clean, accurate workflow.txt that a contributor can follow start-to-finish using only `python src/` commands.
</objective>

<execution_context>
@D:\Report_verifier\.planning\phases\06-documentation-alignment\06-CONTEXT.md
@D:\Report_verifier\.planning\phases\06-documentation-alignment\06-PATTERNS.md
</execution_context>

<context>
@D:\Report_verifier\.planning\PROJECT.md
@D:\Report_verifier\.planning\ROADMAP.md
@D:\Report_verifier\CLAUDE.md
@D:\Report_verifier\.planning\codebase\ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite workflow.txt with accurate 6-phase structure</name>
  <files>workflow.txt</files>
  <read_first>
    - D:\Report_verifier\workflow.txt — current stale content (full read to understand what to replace)
    - D:\Report_verifier\CLAUDE.md — authoritative phase commands (Workflow section, lines 22–43)
    - D:\Report_verifier\.planning\codebase\ARCHITECTURE.md — accurate output artifact names and phase goals
  </read_first>
  <action>
Completely replace the contents of workflow.txt. The current file is 123 lines of garbled, partially duplicated content that must not be preserved. Write the following content exactly:

---

# Medical AI Hallucination Detection — Execution Workflow

Run phases in order. Each phase produces artifacts consumed by the next.

## Phase 1 — Data Collection

**Goal:** Build the raw medical knowledge base from PubMed.

```bash
python src/pubmed_fetcher.py
```

**Output:** `data/expanded_knowledge_base.csv` (raw KB, 500+ articles)

**Notes:**
- Set `NCBI_API_KEY` environment variable for 10 req/s instead of 3 req/s.
- Fetches 130+ medical topics with year/journal segmentation to work around NCBI's 10 000-result cap.

---

## Phase 2 — KB Preprocessing and Embedding

**Goal:** Clean, enhance, and embed the knowledge base for runtime retrieval.

```bash
python src/medical_preprocessor.py
```

**Output:**
- `data/expanded_knowledge_base_preprocessed.csv` — quality-graded, NER-annotated KB
- `data/kb_embeddings_preprocessed.npy` — pre-computed SciBERT embeddings (regenerating is slow)

**Notes:**
- Runs offline once. Do not re-run unless the raw KB changes.
- Generates SciBERT embeddings; requires GPU or patience on CPU.

---

## Phase 3 — Claim Extraction (development/test)

**Goal:** Test claim extraction and verification engine in isolation.

```bash
python src/claim_extractor_fixed.py
```

**Output:** Claim extraction results printed to stdout (does not write output files).

**Notes:**
- Runs against the preprocessed KB. Useful during development to verify FAISS index and confidence scoring.

---

## Phase 4 — Full Verification Pipeline

**Goal:** Run the complete hallucination detection pipeline on medical summaries.

```bash
python src/medical_verifier.py
```

**Output:**
- `outputs/medical_verification.json` — structured verification results with risk assessment
- `outputs/medical_verification.csv` — tabular claims export
- `outputs/medical_verification_safety_log.json` — safety validation log

**Notes:**
- Applies 4-layer risk model (medical plausibility → severity count → implausibility → confidence ratio).
- The Responsible AI layer (safety warnings, expert-review flags, disclaimer) is integrated into this phase — it is not a separate step.

---

## Phase 5 — Interactive Analysis

**Goal:** Explore verification results interactively.

```bash
jupyter notebook medical_verification_dashboard.ipynb
```

**Output:** Interactive claim/risk visualization in the browser.

---

## Phase 6 — Report Generation

**Goal:** Generate an HTML report from verification results.

```bash
python src/report_generator.py
```

**Output:** Self-contained HTML report built from `outputs/medical_verification.json`.

---

## Quick Reference

| Phase | Script | Key Output |
|-------|--------|------------|
| 1 | `src/pubmed_fetcher.py` | `data/expanded_knowledge_base.csv` |
| 2 | `src/medical_preprocessor.py` | `data/expanded_knowledge_base_preprocessed.csv`, `data/kb_embeddings_preprocessed.npy` |
| 3 | `src/claim_extractor_fixed.py` | stdout only |
| 4 | `src/medical_verifier.py` | `outputs/medical_verification.json` |
| 5 | `medical_verification_dashboard.ipynb` | interactive browser UI |
| 6 | `src/report_generator.py` | HTML report |

---

Do NOT use the following — they do not exist:
- `evaluation_pipeline.py`
- `evaluation_dashboard.ipynb`
- `evaluation_report_generator.py`
- Any `conda run -p` command prefix

---

*Last updated: Phase 6 Documentation Alignment*

---

Critical accuracy rules for this rewrite:
- Use exactly `python src/pubmed_fetcher.py` — no path prefix variations
- Phase 6 command is `python src/report_generator.py` (not evaluation scripts)
- Responsible AI layer is a note under Phase 4, not a separate phase
- Output file for Phase 1 is `data/expanded_knowledge_base.csv` (not `knowledge_base.csv`)
- Output files for Phase 2: `data/expanded_knowledge_base_preprocessed.csv` AND `data/kb_embeddings_preprocessed.npy`
- Output files for Phase 4: three files (json, csv, safety_log.json)
  </action>
  <verify>
    <automated>python -c "
content = open('D:/Report_verifier/workflow.txt').read()
checks = [
    ('python src/pubmed_fetcher.py' in content, 'Phase 1 command present'),
    ('python src/medical_preprocessor.py' in content, 'Phase 2 command present'),
    ('python src/claim_extractor_fixed.py' in content, 'Phase 3 command present'),
    ('python src/medical_verifier.py' in content, 'Phase 4 command present'),
    ('python src/report_generator.py' in content, 'Phase 6 command present'),
    ('conda' not in content, 'No conda prefix'),
    ('evaluation_pipeline' not in content, 'No stale script reference'),
    ('Phase 7' not in content, 'No Phase 7 heading'),
    ('expanded_knowledge_base_preprocessed.csv' in content, 'Correct Phase 2 output artifact'),
    ('medical_verification.json' in content, 'Correct Phase 4 output artifact'),
]
failed = [msg for ok, msg in checks if not ok]
if failed:
    print('FAILED:', failed)
    exit(1)
print('All checks passed')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - workflow.txt contains the string "python src/pubmed_fetcher.py"
    - workflow.txt contains the string "python src/medical_preprocessor.py"
    - workflow.txt contains the string "python src/claim_extractor_fixed.py"
    - workflow.txt contains the string "python src/medical_verifier.py"
    - workflow.txt contains the string "python src/report_generator.py"
    - workflow.txt does NOT contain "conda"
    - workflow.txt does NOT contain "evaluation_pipeline"
    - workflow.txt does NOT contain "evaluation_report_generator"
    - workflow.txt does NOT contain "Phase 7"
    - workflow.txt contains "expanded_knowledge_base_preprocessed.csv"
    - workflow.txt contains "medical_verification.json"
    - workflow.txt contains exactly 6 phase headings (Phase 1 through Phase 6)
  </acceptance_criteria>
  <done>workflow.txt is a clean 6-phase guide using python src/ commands; no conda prefix, no stale scripts, Responsible AI layer noted as integrated into Phase 4 rather than listed as Phase 7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow.txt → developer | Static doc consumed by contributors; no executable code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | workflow.txt | accept | Doc is public; no secrets or credentials are referenced in the workflow guide |
| T-06-02 | Tampering | workflow.txt | accept | Version-controlled; any modification is tracked by git; low-value target |
</threat_model>

<verification>
After task completion, verify workflow.txt:
1. `grep -c "python src/" workflow.txt` — should return 5 (one per script phase)
2. `grep "conda" workflow.txt` — should return no output (empty)
3. `grep "evaluation_pipeline" workflow.txt` — should return no output
4. Count phase headings: `grep -c "## Phase [1-6]" workflow.txt` — should return 6
</verification>

<success_criteria>
A contributor can open workflow.txt, run each phase command verbatim, and produce the documented output artifacts. No command requires conda. No script reference points to a non-existent file. The Responsible AI layer is correctly described as integrated into Phase 4.
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-alignment/06-01-SUMMARY.md` following the template at `@D:\Report_verifier\.planning\phases\06-documentation-alignment\` (or use the standard summary format: phase, plan, what was done, files modified, verification outcome).
</output>
