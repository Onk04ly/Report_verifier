---
phase: 01-contract-and-config-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/medical_config.py
  - src/claim_extractor_fixed.py
requirements:
  - VERI-01
  - VERI-02
autonomous: true
must_haves:
  truths:
    - "Extractor runtime tunables come only from centralized config and invalid config fails fast."
    - "Claim extraction emits the canonical `claim_text` field instead of legacy `text`."
    - "Claim schema is rejected immediately when required keys are missing or misnamed."
  artifacts:
    - path: "src/medical_config.py"
      provides: "validated central runtime config and accessor surface"
    - path: "src/claim_extractor_fixed.py"
      provides: "config-driven extraction and canonical claim payload generation"
  key_links:
    - from: "src/medical_config.py"
      to: "src/claim_extractor_fixed.py"
      via: "full config object injection from get_global_config()"
    - from: "src/claim_extractor_fixed.py"
      to: "src/medical_verifier.py"
      via: "canonical claim payload schema with claim_text"
---

<objective>
Unify the extractor runtime contract around one validated configuration object and one canonical claim payload shape.

Purpose: remove hidden defaults and hardcoded scoring constants from the extractor so the verification pipeline becomes auditable and deterministic.
Output: validated config contract in `src/medical_config.py` and extractor output that only uses `claim_text`.
</objective>

<execution_context>
@C:/Users/welcome/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/welcome/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-and-config-unification/01-CONTEXT.md
@.planning/phases/01-contract-and-config-unification/01-RESEARCH.md
@src/medical_config.py
@src/claim_extractor_fixed.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Move extractor tunables into validated config</name>
  <files>src/medical_config.py, src/claim_extractor_fixed.py</files>
  <read_first>
Read `src/medical_config.py` and the extractor scoring paths in `src/claim_extractor_fixed.py` that currently hardcode thresholds, distance normalization, and penalty values.
  </read_first>
  <behavior>
    - Validate every required numeric field in `ConfigurationSettings.__post_init__` and raise explicit `ValueError` messages that list the invalid key and the bad value.
    - Add the extractor-specific constants currently hardcoded in confidence scoring and retrieval logic to `ConfigurationSettings`.
    - Make `ClaimExtractor.__init__` consume the full object returned by `get_global_config()` and stop constructing a local fallback dict.
  </behavior>
  <action>Replace inline extractor defaults and magic numbers with config reads from `get_global_config()`, and make invalid config states fail immediately with explicit missing/invalid key details per D-01, D-02, D-03, D-04, and D-05.</action>
  <acceptance_criteria>
    - `ClaimExtractor` no longer creates its own local config dict.
    - Every runtime scoring constant used by the extractor is sourced from `ConfigurationSettings`.
    - Invalid config construction raises a precise error before any model load or retrieval work starts.
  </acceptance_criteria>
  <verify>
    <automated>python src/medical_verifier.py</automated>
  </verify>
  <done>Extractor initialization and scoring use centralized validated config only, with no local default contract remaining.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit canonical claim_text and reject schema drift</name>
  <files>src/claim_extractor_fixed.py</files>
  <read_first>
Read the claim dict construction in `identify_medical_claims()` and the summary return path in `extract_claims_from_summary()`.
  </read_first>
  <behavior>
    - Replace the legacy `text` key in every emitted claim dict with `claim_text`.
    - Keep the rest of the claim payload stable, but do not add a compatibility fallback for callers that still expect `text`.
    - Add an explicit schema check before returning extracted claims so malformed claim dicts raise a hard error immediately.
  </behavior>
  <action>Change the extractor output boundary to enforce the canonical `claim_text` contract only, with hard failures on missing required keys per D-07, D-08, D-09, D-10, and D-11.</action>
  <acceptance_criteria>
    - Every claim returned by the extractor uses `claim_text`.
    - Missing required claim fields raise a `ValueError` instead of being tolerated.
    - No dual-key compatibility branch remains in the extractor output path.
  </acceptance_criteria>
  <verify>
    <automated>python src/medical_verifier.py</automated>
  </verify>
  <done>Extractor claim payloads are canonical and schema-validated at the output boundary.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config object -> extractor runtime | untrusted or invalid config values can change scoring and retrieval behavior |
| extractor output -> verifier input | malformed claim dicts can break downstream verification or silently drift output shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `src/medical_config.py` | mitigate | Validate required config invariants in `__post_init__` and fail fast with explicit missing/invalid field messages. |
| T-01-02 | T | `src/claim_extractor_fixed.py` | mitigate | Remove inline defaults and require the centralized config object so runtime thresholds cannot drift. |
| T-01-03 | T | `src/claim_extractor_fixed.py` | mitigate | Enforce `claim_text`-only output and raise on schema mismatch before claims leave the module. |
</threat_model>

<verification>
- Run `python src/medical_verifier.py` after each task to confirm the runtime path still initializes and emits contract-shaped results.
- Confirm the extractor no longer contains a local fallback config dict or `text` emission in the claim payload.
- Confirm invalid config states are rejected before downstream retrieval or scoring begins.
</verification>

<success_criteria>
- Central config is the only runtime source for extractor tunables.
- `ConfigurationSettings` rejects invalid or missing values immediately.
- Extractor output uses only `claim_text` and fails hard on schema drift.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-and-config-unification/01-01-SUMMARY.md`
</output>
