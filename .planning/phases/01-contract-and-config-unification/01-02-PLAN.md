---
phase: 01-contract-and-config-unification
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/medical_verifier.py
  - tests/conftest.py
  - tests/test_contracts.py
requirements:
  - VERI-02
  - VERI-03
autonomous: true
must_haves:
  truths:
    - "Verifier rejects any claim payload that does not contain canonical `claim_text`."
    - "Verification results export the same deterministic structure for single and batch flows."
    - "Focused contract tests pin config, schema, and export guardrails."
  artifacts:
    - path: "src/medical_verifier.py"
      provides: "schema validation at verifier boundary and deterministic export shape"
    - path: "tests/test_contracts.py"
      provides: "focused config/schema/export regression tests"
    - path: "tests/conftest.py"
      provides: "shared fixtures for synthetic claim payloads"
  key_links:
    - from: "src/medical_verifier.py"
      to: "tests/test_contracts.py"
      via: "contract behavior exercised by pytest guards"
    - from: "src/medical_verifier.py"
      to: "src/claim_extractor_fixed.py"
      via: "strict claim_text schema validation before risk and export logic"
---

<objective>
Make the verifier enforce the canonical claim schema at its input boundary and lock the exported result shape with focused regression tests.

Purpose: prevent downstream callers from relying on legacy claim keys while keeping JSON/CSV/safety-log output deterministic.
Output: verifier boundary validation, deterministic export structure, and pytest contract coverage for VERI-02 and VERI-03.
</objective>

<execution_context>
@C:/Users/welcome/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/welcome/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-and-config-unification/01-CONTEXT.md
@.planning/phases/01-contract-and-config-unification/01-RESEARCH.md
@.planning/phases/01-contract-and-config-unification/01-VALIDATION.md
@src/medical_verifier.py
@src/claim_extractor_fixed.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce claim_text at verifier input boundary</name>
  <files>src/medical_verifier.py</files>
  <read_first>
Read the verifier claim-processing paths in `verify_single_summary()`, `_assess_overall_risk()`, `export_results()`, and `verify_from_claim_extractor_json()`.
  </read_first>
  <behavior>
    - Add an explicit schema validator that requires `claim_text` and the current required claim fields before any risk or safety logic runs.
    - Remove every `claim['text']` access in verifier code and replace it with strict `claim_text` reads.
    - Raise a hard error on malformed claim payloads instead of silently falling back to legacy keys.
  </behavior>
  <action>Update the verifier to treat schema mismatch as an immediate contract failure per D-08, D-09, and D-11, and update any immediate caller logic in `main()` if it still depends on the legacy shape per D-14.</action>
  <acceptance_criteria>
    - Verifier input validation rejects payloads without `claim_text`.
    - Risk assessment and export logic consume only canonical claim keys.
    - The module no longer contains fallback logic between `text` and `claim_text`.
  </acceptance_criteria>
  <verify>
    <automated>python src/medical_verifier.py</automated>
  </verify>
  <done>Verifier boundary enforcement is strict and canonical, with no silent schema compatibility path remaining.</done>
</task>

<task type="auto">
  <name>Task 2: Build focused contract fixtures and tests</name>
  <files>tests/conftest.py, tests/test_contracts.py</files>
  <read_first>
Read `01-VALIDATION.md` and derive fixtures from the canonical claim payload and export result shapes used by the runtime.
  </read_first>
  <behavior>
    - Add a minimal fixture set for valid claims, invalid claims, and a deterministic verification result payload.
    - Write one test for config validation, one for strict `claim_text` schema enforcement, and one for deterministic export structure.
    - Keep the tests narrow and deterministic so they run as the phase gate without requiring external services.
  </behavior>
  <action>Create `tests/conftest.py` and `tests/test_contracts.py` to pin VERI-01/02/03 behavior with focused `pytest` coverage that mirrors the validation map in `01-VALIDATION.md`.</action>
  <acceptance_criteria>
    - Test fixtures provide canonical valid and invalid claim dictionaries.
    - Tests fail if the extractor or verifier accepts legacy `text` payloads.
    - Tests fail if export structure changes in a way that breaks downstream consumers.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_contracts.py -x</automated>
  </verify>
  <done>Focused regression tests exist and pin the phase contract behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm runtime smoke path and export determinism</name>
  <files>src/medical_verifier.py</files>
  <read_first>
Re-read the smoke path in `main()` and the JSON/CSV export branch in `export_results()` after the verifier boundary changes land.
  </read_first>
  <behavior>
    - Ensure the smoke path still runs end to end with the canonical claim schema.
    - Keep export output keys stable across JSON and CSV branches, including the safety log.
    - Remove any output formatting branch that depends on the legacy claim field name.
  </behavior>
  <action>Stabilize the verifier smoke workflow so the phase gate command set from `01-VALIDATION.md` passes with the new strict schema and deterministic export structure.</action>
  <acceptance_criteria>
    - `python src/medical_verifier.py` completes without contract regressions.
    - Exported JSON/CSV outputs use the same deterministic claim fields.
    - Safety log creation still occurs alongside exports.
  </acceptance_criteria>
  <verify>
    <automated>python src/medical_verifier.py</automated>
  </verify>
  <done>Verifier smoke execution and export behavior are stable after the contract change.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claim dicts -> verifier risk layer | untrusted claim payloads can tamper with risk assessment if schema checks are missing |
| verifier exports -> downstream consumers | unstable export structure can mislead evaluators or break analysis scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `src/medical_verifier.py` | mitigate | Validate `claim_text` and required claim keys before risk assessment and export processing. |
| T-01-05 | R | `src/medical_verifier.py` | mitigate | Keep export keys deterministic and safety-log generation unconditional so outputs are auditable. |
| T-01-06 | I | `tests/test_contracts.py` | accept | The fixtures use synthetic claims only; no sensitive data is introduced into regression tests. |
</threat_model>

<verification>
- Run `pytest tests/test_contracts.py -x` and keep it green before closing the phase.
- Run `python src/medical_verifier.py` to satisfy the phase smoke gate and confirm no schema or export regressions.
- Confirm verifier code no longer references legacy `claim['text']` access.
</verification>

<success_criteria>
- Verifier rejects malformed claim payloads at the boundary.
- Exported verification output is deterministic and stable for downstream consumers.
- Phase gate commands from `01-VALIDATION.md` pass before the phase is considered complete.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-and-config-unification/01-02-SUMMARY.md`
</output>
